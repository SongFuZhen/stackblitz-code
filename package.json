{
  "name": "node-starter",
  "version": "0.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "browserify": "browserify ./index.js -o ./dist/bundle.js"
  },
  "dependencies": {
    "browserify": "^17.0.0"
  }
}
